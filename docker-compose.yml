version: '3.8'

services:
  db:
    image: postgres:14-alpine
    container_name: pied_pipper_db
    restart: always
    environment:
      POSTGRES_DB: pied_pipper_db
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    ports:
      - "5432:5432"